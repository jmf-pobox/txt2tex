// Example: Schema Definitions
// Demonstrates basic schema syntax
// From USER_GUIDE.md - Schema Notation section

// Note: Anonymous schemas (schema without name) are not supported by fuzz
// All schemas must have names

schema Point
  x : N
  y : N
where
  x + y = 10
end
