// Example: Free Types
// Demonstrates basic free type definitions
// From USER_GUIDE.md - Definitions section

IntTree ::= leaf | node<N cross IntTree cross IntTree>

axdef
  emptyTree : IntTree
  treeDepth : IntTree -> N
where
  emptyTree = leaf
  treeDepth(leaf) = 0
end

// Note: Generic free types have limitations in fuzz
// For truly generic tree types, use parametric definitions with given types
