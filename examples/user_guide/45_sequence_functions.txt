// Example: Sequence Types
// Demonstrates sequence type declarations
// From USER_GUIDE.md - Sequences section

given X

axdef
  s : seq X
  t : seq X
where
  s ^ t = s ^ t
end

// Note: Sequence functions (head, tail, last, front, rev) are not
// included in the fuzz standard library and must be defined if needed
