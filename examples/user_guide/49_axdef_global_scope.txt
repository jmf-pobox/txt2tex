// Example: Axdef - Global Scope
// Demonstrates axdef blocks with global identifiers
// From USER_GUIDE.md - Schema Notation section

zed
  given ShowId, EpisodeId
end


axdef
  podcasts : F ShowId
  podcast_episodes : ShowId +-> F EpisodeId
where
  podcasts subset dom podcast_episodes
end

TEXT: Later in document we can reference podcast_episodes globally:

zed
  Answer == {s : dom podcast_episodes | # (podcast_episodes(s)) > 5}
end

