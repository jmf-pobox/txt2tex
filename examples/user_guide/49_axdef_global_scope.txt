// Example: Axdef - Global Scope
// Demonstrates axdef blocks with global identifiers
// From USER_GUIDE.md - Schema Notation section

zed
  given BookId, ChapterId
end


axdef
  books : F BookId
  book_chapters : BookId +-> F ChapterId
where
  books subset dom book_chapters
end

TEXT: Later in document we can reference book_chapters globally:

zed
  Answer == {b : dom book_chapters | # (book_chapters(b)) > 5}
end

