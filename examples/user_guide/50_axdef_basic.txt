// Example: Axiomatic Definitions
// Demonstrates basic axdef with constraints
// From USER_GUIDE.md - Schema Notation section

axdef
  population : N
where
  population > 0
end

axdef
  x : N; y : N
where
  x > y
end

// Note: Generic axdef (axdef [T]) is not supported by fuzz
// Use gendef for generic definitions instead
