// Example: Compound Identifiers with Postfix Operators
// Demonstrates using +, *, ~ in identifier names
// From USER_GUIDE.md - Relations section

R+ == {a, b : N | b > a}

R* == R+ union id[N]

R~ == inv R

schema S+
  x : N
end

rel_1+ == {x, y : N | x < y}
