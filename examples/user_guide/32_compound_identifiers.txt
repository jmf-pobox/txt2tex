// Example: Multi-Word Identifiers
// Demonstrates identifiers with numbers and letters
// From USER_GUIDE.md - Relations section

zed
  Rplus == {a, b : N | b > a}
end


zed
  Rstar == Rplus union id N
end


schema S1
  x : N
end

rel1plus == {x, y : N | x < y}

// Note: Postfix operator symbols (+, *, ~) cannot be used as part of
// identifier names in fuzz. Use descriptive names like "plus" instead
