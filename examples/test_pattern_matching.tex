\documentclass{article}
\usepackage{zed-cm}
\usepackage{zed-maths}
\usepackage{zed-proof}
\usepackage{amsmath}
\begin{document}

\bigskip
\noindent
\textbf{Test Pattern Matching}

\medskip

\begin{axdef}
\where
\forall x \colon Entry \bullet \forall s \colon \seq~Entry \bullet courses(\langle \rangle) = \langle \rangle \land courses(\langle x \rangle \cat s) = (\text{if } x.3 = 479 \text{ then } \langle x \rangle \cat courses(s) \text{ else } courses(s))
\end{axdef}

\end{document}