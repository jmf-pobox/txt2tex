** Test Pattern Matching **

given Entry

axdef
  filter : seq Entry -> seq Entry
where
  filter(<>) = <>
  forall x : Entry; s : seq Entry | filter(<x> ^ s) = <x> ^ filter(s)
end

TEXT: This demonstrates pattern matching on sequences with base case (<>) and recursive case (<x> ^ s).
