=== Bug 3 Test: Compound Identifier with Operator Suffix ===

TEXT: The abbreviation below should define R+ as a relation, but fails because the lexer tokenizes it as "R" followed by "+".

zed
  R+ == {a, b : N | b > a}
end
